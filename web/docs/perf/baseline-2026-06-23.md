# Performance baseline — 2026-06-23

Captured with `web/scripts/perf-measure.mjs` against local dev (`http://localhost:3000/`), iPhone-13 mobile profile. Dev build (prod differs slightly, but media payload is identical).

## Homepage `/`

| Metric | Baseline | Target (U6 re-measure) |
| --- | --- | --- |
| Initial transfer (before scroll) | **13.49 MB** (41 req) | < 2 MB |
| Full transfer (after scroll) | **29.04 MB** (48 req) | < 5 MB |
| Image bytes | 20.68 MB | < 2.5 MB |
| Media (video) bytes | 7.40 MB | < 2 MB |
| Script bytes | 0.79 MB | ~same |
| LCP (localhost) | 1016 ms | keep < 2.5 s on mobile/throttled |
| Load → networkidle | 11.9 s | large drop |

## Top assets (baseline)

```
4.50 MB  media  /imersiv/savapass-ticket-engine-loop.mp4   (intro-video, autoplay → full download)
2.90 MB  media  /imersiv/savapass-hero-loop.mp4            (hero-video, autoplay → full download)
2.71 MB  image  /imersiv/year-2024.png
2.71 MB  image  /imersiv/event-easter.png                  (EAGER)
2.68 MB  image  /imersiv/year-2026.png
2.49 MB  image  /imersiv/year-2025.png
2.42 MB  image  /imersiv/team-interact.png
2.38 MB  image  /imersiv/stat-community.png
2.26 MB  image  /imersiv/event-cupid.png                   (EAGER)
2.12 MB  image  /imersiv/stat-concert.png
0.75 MB  image  /imersiv/stat-scan.png
0.16 MB  image  /imersiv/echoes-unplugged.png              (phone poster, eager — correct)
```

## Notes / findings

- **Orphan files (not loaded by the homepage):** `step-event.png` (5.6 MB), `step-pay.png` (3.4 MB), `step-qr.png` (3.6 MB), `savapass-disintegrate.mp4` (4.8 MB). ~17 MB of dead weight in `web/public/imersiv/` — safe to delete.
- Videos autoplay, so `preload="metadata"` does not prevent the full download. Re-encoding bitrate/resolution is the lever (background loops tolerate modest quality).
- `event-easter.png` / `event-cupid.png` are eager despite being below the fold → make lazy.
- Middleware (`proxy.ts`) is scoped to auth routes only → not a homepage cost.
- Data layer: homepage does a live `getActiveEvent` query each load (capped 2 s); no caching (addressed in U5).
