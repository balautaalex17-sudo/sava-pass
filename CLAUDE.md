# SavaPass

## About

Digital ticketing app for **Interact Sf. Sava** (school club, Bucharest). Mobile-first event pass: view event → buy ticket → receive QR → scan at the door. Inspired by DICE / Apple Wallet / Stripe Checkout.

**Current state: Full production app built in `web/`** (Next.js 16 + Supabase + Stripe + Resend). All 6 build phases complete. Prototype at root is preserved as visual reference only.

Implemented screens (visual only, no real backend):
- Mobile app: Home, Event Details, Checkout, Payment Success, My Ticket (QR), Scanner, Admin Dashboard
- Desktop website: brand landing with hero, "how it works", events grid, about Interact, footer
- Mobile-web view: site adapted for phone (used as deep link from shared tickets)

See `PRODUCT.md` for the product vision + design system, `HANDOFF.md` for the technical handoff (code structure, data contract, path to production), and `chats/` for the full design intent (raw transcripts from the design tool).

## Tech stack

**Production app (`web/`):**
- Next.js 16 (App Router, server actions, Turbopack)
- Tailwind v4 (`@theme` CSS variables in `globals.css`)
- Supabase (Postgres, RLS, Realtime, Auth) — project `shzyvrojbtbczqqoilip`
- Stripe Checkout (RON, API version `2026-05-27.dahlia`)
- Resend (transactional email)
- HMAC-SHA256 QR token format: `SP1.<ticketId>.<base64url(hmac)>`
- `@zxing/browser` for camera QR decode on scanner page

**Prototype (root):**
- React 18 UMD + Babel-in-browser, no build step, plain CSS tokens

## How to work in here

CDN dependencies + `<script src="src/...">` means **opening the file directly will likely fail** in modern browsers (CORS on `file://`). Serve locally:

```bash
cd projects/sava-pass
python -m http.server 8080
# then open http://localhost:8080/SavaPass%20Prototype.html
```

Or `npx serve .` if Python isn't installed.

When editing one screen, grep for its name first — don't load the whole 56KB `screens-mobile.jsx` into context for a one-screen change.

### Next steps (for real product)

The prototype is the *visual contract*. The full build plan lives in `PLAN.md`: stack is **Next.js 15 + Supabase + Stripe + Resend**, production app goes in `web/`, built in 6 phases (scaffold → public pages → purchase → scanner → admin → launch) with a ready-to-paste Claude Code prompt per phase. Follow PLAN.md §9 prompts in order, one per session, commit after each.

## Architecture

Single HTML entry loads React UMD + Babel from CDN, then `<script type="text/babel" src="src/...">` tags pull in the JSX modules. Babel transforms them in the browser. No bundler involved.

## Conventions

```
SavaPass Prototype.html     entry, opens directly in browser
src/                        12 JSX/CSS modules (components, screens, app composition)
assets/                     event posters, Interact logos
uploads/                    pasted/drawn images from design iteration (reference, can remove later)
chats/                      raw design-tool transcripts (chat1 = app + spec, chat2 = landing)
PRODUCT.md                  product vision + design system (merged master)
HANDOFF.md                  technical handoff: code structure, data contract, prod path
PLAN.md                     full-stack build plan: PRD, schema, flows, phases, Claude Code prompts
web/                        (future) production Next.js app — prototype at root stays as reference
.design-canvas.state.json   design tool state (preserved for re-import)
active/                     temp/scratch (gitignored)
.cloud/, .claude/           Claude tools/config
```

## Lessons learned

- `claude.ai/design/p/<id>?...&via=share` share URLs return **403** to WebFetch (auth-gated) and the `api.anthropic.com/v1/design/h/<id>` handoff form 404s for these. Can't pull a live design programmatically — work from the extracted bundle in `active/savapass-design/` or ask the user to re-export / paste the files.
- The extracted bundle in `active/savapass-design/` is an **older snapshot** than the live design. The user's newer HANDOFF.md references files that don't exist locally (`Flow Video.html`, `membership.jsx`, `animations.jsx`, `flow-video.jsx`, standalone `export/` bundles). When a handoff doc lists files, verify they exist before assuming the merge is complete.
- Master docs are now `PRODUCT.md` (product + design system) and `HANDOFF.md` (technical). The old generic `HANDOFF_README.md` was removed (folded into PRODUCT.md §1).
- **Supabase type stubs go stale immediately.** Always generate real types with `supabase gen types` (or the MCP `generate_typescript_types` tool) before writing code that calls `.from()` — manual stubs cause `never[]` insert errors that are hard to debug.
- **Next.js 16 breaking changes vs 15:** (1) `middleware.ts` → `proxy.ts`, function must be named `proxy`; (2) Stripe API version was `2025-04-30.basil`, now must be `"2026-05-27.dahlia"` (SDK v22+); (3) Zod v4 renamed `errorMap` to `error` in `.literal()` second arg.
- **`Buffer` is not `BodyInit`** in this TypeScript config. When returning a PNG from a route handler, wrap with `new Uint8Array(buffer)` before passing to `new Response(...)`.
- **`useSearchParams()` always needs `<Suspense>`** wrapping in Next.js App Router or the static build fails. Split the page into an inner component that uses the hook + an outer default export that wraps it in `<Suspense>`.
- `event_stats` view columns are nullable (`sold: number | null`, `checked_in: number | null`) in generated types — always null-coalesce with `?? 0` before arithmetic comparisons.
- **The "3 missing env keys" claim was wrong — it's 5.** `.env.local` had `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, `QR_SIGNING_SECRET` AND the whole Stripe trio (`pk_test`/`sk_test`/`whsec`) as `*_REPLACE_ME` placeholders. Masked-prefix checks (`pk_test...`) are misleading because the placeholder literally starts with `pk_test_`. Always grep `.env.local` for `REPLACE_ME` to count real gaps.
- **The scary security risks flagged in review were already handled in code (2026-06-11 audit):** scanner check-in is already atomic (`update().eq("status","valid").select()` → 0 rows = already_in, `scanner/actions.ts`); `tickets` RLS is staff-only SELECT (`is_staff()`), no anon read, so no ticket enumeration; Stripe webhook early-returns if order already `paid`. Buyer ticket view at `/bilet/[token]` works via service-role + HMAC token, not an anon RLS policy. Verify code before trusting a review's "this is probably broken."
- **Supabase MCP cannot return the `service_role` secret, but the Supabase CLI can** — this machine is logged in (token in Windows Credential Manager), so `npx -y supabase projects api-keys --project-ref shzyvrojbtbczqqoilip -o json` returns the real service_role key with no dashboard visit. Stripe/Resend keys still need the user to paste them (no logged-in CLI for those).
- Live DB already seeded: active event `echoes-unplugged` (4500 bani / 120 cap) plus two `past` events. Use it for end-to-end testing, no seeding needed.
- **"Eroare internă" on checkout was the `orders` insert failing with the placeholder service-role key** (fixed 2026-06-11: real key + generated `QR_SIGNING_SECRET` written to `web/.env.local`). Stripe trio + Resend still placeholders. With a fake Stripe key the action now fails cleanly at session-create with "Plata nu a putut fi inițiată" and marks the order `failed`.
- Next.js dev hot-reloads `.env.local` edits (logs `Reload env`) — no server restart needed after changing env values.
- **Server-action forms are curl-testable without a browser:** GET the page, copy the rendered hidden inputs (`$ACTION_REF_1`, `$ACTION_1:0`, `$ACTION_KEY`) into a multipart POST alongside the visible fields. The response HTML contains the returned action state. (Next 16 uses these names, not the older `$ACTION_ID_<hash>`.)
- In server actions, keep `redirect()` **outside** try/catch — it throws `NEXT_REDIRECT` internally, so a catch around it swallows the redirect.
- **Success page stuck on "Se emite biletul…" then "Ceva a mers greșit" = webhook never delivered.** The page polls `/api/order-status` 15× / 2s; order stays `pending` because nothing marked it `paid`. Root cause is almost always `stripe listen` not running (separate long-lived process) — grep the dev-server log for `POST /api/webhooks/stripe`; zero hits = listener down.
- **The `whsec_` from `stripe listen` is `whsec_` + 64 hex = 70 chars total.** A pasted value of len 69 is truncated by one char and silently fails signature verification (route returns 400, order never completes). Verify exact match, don't eyeball the prefix. Print it non-interactively with `stripe listen ... --print-secret`.
- **`stripe listen` is stable per device** — the signing secret doesn't rotate between runs, so once it's correct in `.env.local` it stays correct. Stripe CLI installed via `winget install Stripe.StripeCli`; binary at `%LOCALAPPDATA%\Microsoft\WinGet\Packages\Stripe.StripeCli_*\stripe.exe` (PATH needs a shell restart after install).
- **Replay a missed webhook without re-paying:** `stripe events list --limit 5` to find the `checkout.session.completed` (its `data.object.metadata.order_id` ties it to the stuck order), then `stripe events resend <evt_id>` — it forwards to the running `stripe listen`. Completed the stuck `pending` order this way (2026-06-11).
- Stripe account on this machine is `acct_1Th7kwPBEvqABTYx` (display name "Alex"). The Stripe MCP is connected but **cannot return API keys** (Stripe never exposes `sk_`/`pk_` via API by design) — keys come from the dashboard, webhook secret from the CLI.
- Resend `from` is `noreply@savapass.ro` (unverified domain) — the ticket email will likely be rejected by Resend even though the webhook returns 200 (it doesn't check the send result). In-app ticket at `/bilet/<token>` works regardless. Verify a domain in Resend before trusting email delivery.
- **Supabase-generated `types.ts` can have escaped quotes (`\"`) in the CompositeTypes utility section**, causing TS1127 parse errors. Always check `lib/supabase/types.ts` for `\"` after regenerating types and replace with plain `"`.
- **Email-based RLS is the right strategy for retroactive ticket linking.** Instead of joining on `user_id` FK (which requires backfill), add a policy `USING (auth.jwt()->>'email' = holder_email)` — past tickets appear on first sign-in automatically.
- **Buyer route handling in proxy.ts:** for `/conta/*`, only refresh the session cookie in the middleware and let the page do its own `redirect('/conta/login')` via `getUser()` — avoids double-redirects and keeps auth logic centralized in the page.

- **Visual work follows `docs/REDESIGN-SPEC.md`, not the design skills directly.** The spec (written 2026-06-12) already resolves every PRODUCT.md-vs-skill conflict (eyebrows = data labels only, em dash/· sanctioned, Instrument Serif kept at 3 ceremonial moments, backdrop-blur removed everywhere, scanner stays dark but flat navy). Implementer agents read only the spec + their batch files.
- Audit found shipped violations of PRODUCT.md worth remembering: blurred navs (PRODUCT.md allows blur in one place), radial-gradient scanner bg (page gradients banned), emoji in AdminClient/conta-login (no emoji rule), 6px gradient side-stripe on conta ticket cards. The spec's batch C/D kill all four.
- **Batch A redesign complete (2026-06-12).** Motion system installed in globals.css (tokens, keyframes, utility classes, scroll-reveal pattern). Shared components reworked: Button (pressable/hover-dim classes, no more useState press), Chip (dot=false default, size sm), Logo (logo-gear class for hover spin), new Reveal.tsx (IntersectionObserver scroll-reveal), new GearWatermark.tsx. Landing page redesigned: blur removed, eyebrows killed above headings, hero stats strip deleted, TrustBand slimmed to inline strip, HowItWorks cards replaced with transparent columns + mono numerals, EventsGrid inner CTA block replaced with price+arrow row, AboutInteract navy stat card deleted (replaced with inline stat ledger), pillars converted from grid to pillar list.
- **Polymorphic `as` ref typing in Next.js 16 TS strict mode:** `@ts-expect-error` fails when TS can actually type-check the expression. Use `ref as React.Ref<HTMLDivElement>` cast on polymorphic component refs — cleaner and does not trigger unused-directive error.
- **Batch B redesign complete (2026-06-12).** Buyer flow redesigned per REDESIGN-SPEC.md Batch B: event page (blur removed, eyebrows replaced with sentence-case h2s, content column narrowed to 440, program/perks staggered with anim-rise-fast, poster chip updated, seats chip dot only on warning), BuyCta (blur removed, label format "Cumpără bilet · N RON" with mono price, entrance anim-rise 120ms delay), checkout (eyebrow deleted, context line added, onFocus/onBlur JS deleted replaced with .input class, field rows staggered, general error gets anim-shake anim-fade), success page (Loader2 replaced with gear SVG spinner + anim-spin-slow, ready state choreographed with serif italic h1 "Ești înăuntru." + ring-pulse + staggered anim delays, error state gets GearWatermark), ticket page (anim-pop entrance, used-state header gradient to slate ramp, void ANULAT label gets anim-shake, LiveClock inserted under QR code, footer line anim-fade delay 200ms). New component: LiveClock.tsx (client, pulsing dot + HH:MM:SS ticking clock).
- **`zIndex` with CSS var tokens in JSX:** TypeScript complains about `zIndex: "var(--z-nav)"` (string vs number). Use `"var(--z-nav)" as unknown as number` cast — the CSS var resolves at runtime, the cast satisfies the type checker without changing behavior.
- **Batch C redesign complete (2026-06-12).** Account/auth/system pages redesigned per REDESIGN-SPEC.md Batch C: conta page (eyebrow removed, greeting h1 with firstName, stat boxes quieted to mono 24/700 + sentence-case labels, TicketCard rebuilt as boarding-pass silhouette with dashed divider + stub zone + notch circles, EmptyState gets GearWatermark, SignOutButton hover shifts to danger), conta/login (emoji removed, MailCheck icon with anim-pop in sent state, anim-fade state swap, .input class, footnote · separator), staff login + invite (anim-rise card, 3px navy top border, inputs get .input class, Button variant="navy", anim-shake on errors), error.tsx (GearWatermark + anim-fade, shared Button + Link with .btn classes), not-found.tsx (GearWatermark, 72px serif 404, anim-rise, Link with .btn classes).
- **`Button` component does not support `as` prop** — when needing a link styled as a button, apply `.btn .btn--primary pressable hover-dim` classes directly on a Next.js `<Link>` component. Do not try to pass `as={Link}` to `Button`.
- **Batch D scanner redesign complete (2026-06-12).** Scanner redesigned per REDESIGN-SPEC.md Batch D: flat `--brand-navy` page bg (no gradient), solid dark aside (no blur), verdict choreography split by type (ok = anim-pop + ring-pulse green, warning = anim-pop only, error = anim-pop circle + anim-shake inner), local scale-in/fade-in keyframes deleted and replaced with global .anim-pop/.anim-fade, scan-line glow box-shadow removed and local scan-line keyframe deleted (global .anim-scanline used), all Romanian display strings fixed with correct diacritics (Deja înăuntru, Scanează, Verific…, Pornesc camera…, etc.), disabled buttons at 0.4 opacity, manual-code input gets onFocus/onBlur border-color transition for dark-mode focus ring. Other Batch D files (admin, events, team, statistici) were already spec-compliant.
- **Scanner verdict choreography pattern:** use an IIFE `{state === "result" && verdict && (() => { ... })()}` to compute animation classes inline from verdict.result without a wrapper component — keeps the JSX readable when the class set branches on 3+ conditions.
- **Fix-pass complete (2026-06-12).** All P1 items fixed (exclamation mark, diacritics, spring violations, Button loading keeps children, LiveClock hydration). All P2 items fixed except [verify] ones (font-weight 850/900/750 → 800/700, Button ghost hover uses color shift not filter, HowItWorks stagger uses data-armed transition-delay, checkout h1/context line restored, checkout fields staggered, TicketCard stagger moved to Link, hover shadow for ticket-card, SignOutButton hover via CSS, admin Live chip pulse, StatusControl 3 items, EventEditor full spec block, refresh-link CSS). P3 mechanical items fixed (strokeWidth={1.75} on scanner icons, "..." → "…", nav link hover transition, refresh-link CSS).
- **Landing v2 complete (2026-06-12).** Hero no longer uses event posters as background — the posters are only 340x465px (stretched to w=3840 they were the page's biggest visual problem). Brand surfaces now use real photography stored locally at `web/public/landing/` (hero.jpg = concert crowd, about.jpg = students planning; downloaded from Unsplash, stored locally to avoid next.config remotePatterns + broken-link risk). Posters remain on event cards only (their natural size). New motion: line-mask headline reveal (`.line-mask` + `sp-line-rise`), Ken Burns settle on hero photo (`.anim-zoom-settle`), drawing rail above HowItWorks steps, generic `.rstag` Reveal-stagger child class, scroll-driven nav shadow (`@supports (animation-timeline: scroll())`, zero JS), About photo scale-settle on reveal. New client components: `components/landing/TiltCard.tsx` (pointer tilt, CSS vars via ref + rAF, no setState) and `CountUp.tsx` (IO + rAF textContent mutation, server-renders final value).
- **Continuous-value animation pattern (landing):** pointer/scroll-driven values write CSS custom properties or textContent directly via ref inside rAF — never React state. Both TiltCard and CountUp gate themselves on `prefers-reduced-motion` and (for tilt) `(hover: hover) and (pointer: fine)` via matchMedia at mount.
- **Kill the dev server before `npm run build`** — both write to `.next/` (Turbopack). Stop via `Get-NetTCPConnection -LocalPort 3000` → `Stop-Process`, build, restart dev.
- **Chip component extended with `pulse` prop** — adds `anim-pulse-dot` class to the dot span when `pulse={true}`. Used for the admin Live chip. The `dot` prop must still be `true` for the dot to render.
- **EventEditor sticky save bar pattern:** use `position: sticky; bottom: 0` inside the form with `paddingBottom` on the form to prevent content being hidden behind the bar. The bar uses `margin: 0 -20px` to break out of the grid container's padding.
- **Manrope max weight is 800** — font-weight 850/900/750 all cause faux-bold synthesis. Any value above 800 gets silently clamped; use 800 (bold displays) or 700 (body/labels).
- **The current immersive landing is v3 in the separate `projects/Sava Pass #2` folder, NOT v2.** `web/app/imersiv/content.ts` is an orphaned verbatim port of `SavaPass Immersive v2.html` (different class system) with no `page.tsx` rendering it — dead. When wiring the immersive design to the backend, source from `Sava Pass #2/SavaPass Immersive v3.html` only. Plan: `docs/plans/2026-06-22-001-feat-immersive-v3-landing-backend-plan.md` (v3 → homepage `/`, verbatim port, route-lifetime in-page `<style>` for isolation so v3's global `body`/`:root`/`.btn` rules don't leak to other routes — same pattern the current `page.tsx` LandingStyles already uses).
- **v3 immersive landing is now LIVE on the homepage `/` (built + verified 2026-06-22).** Ported verbatim via `web/scripts/extract-immersive.mjs` (reads `Sava Pass #2/SavaPass Immersive v3.html`, splits style/markup/scripts, applies 3 mechanical edits: font tokens→next/font vars, `assets/`→`/imersiv/`, purchase CTAs→anchors with `__CTA_HREF__`). Outputs `web/app/_immersive/content.ts` (IMMERSIVE_CSS + IMMERSIVE_MARKUP as JSON-stringified strings) + `web/public/imersiv/engine.js` + `engine-motion.mjs`. `web/app/page.tsx` renders `<style>`+markup (dangerouslySetInnerHTML)+`<ImmersiveEngine/>` (client loader: sequential CDN lenis/gsap/scrolltrigger → engine.js → motion.mjs, with Lenis/ScrollTrigger teardown on remount). Old landing archived at `web/app/_archive/landing-v2.tsx`. **Extracting programmatically (not retyping ~1500 lines through the model) is the byte-faithful + token-cheap way to do a verbatim port — re-run the script to re-sync if v3 changes.**
- **Isolation verified working (R4):** route-lifetime in-page `<style>` confirmed — on `/`, `body` bg = `rgb(7,10,18)` (v3 `--ink`); on `/conta/login`, `rgb(248,250,252)` (app slate-50), `.sp-immersive-root`/`.intro` gone. The globals.css `.btn`/`body` collision is benign because the in-page `<style>` renders later in the cascade and wins every visible property. Engine runs clean (lenis/gsap/scrolltrigger true, 20/20 icons, count-up 80/180/264, QR 333 cells, **zero console errors**) via the playwright-skill script `verify-immersive.js`.
- **Supabase project `sava-pass` (ref shzyvrojbtbczqqoilip) auto-pauses to `INACTIVE` after inactivity (free tier).** When paused, `getActiveEvent()`/`getEventBySlug()` return null AND the server hangs ~5s per request before failing → homepage CTAs fall back to `#event`, `/[slug]` pages 404, every `GET /` logs `application-code: 5.0s`. This is NOT a code bug. Restore via Supabase MCP `restore_project` then poll MCP `get_project` until `ACTIVE_HEALTHY`. **Restore takes ~2 min and during `COMING_UP` the platform schemas (auth/extensions/vault) come back BEFORE the `public` app tables — an early `public_tables = 0` read is mid-restore, not data loss; re-check.** Data fully returns: active `echoes-unplugged` (4500 bani = 45 RON, cap 120) + past `cupids-hex`/`easter-egg-hunt`. After restore the homepage serves in ~0.2s.
- **The base app schema (events/orders/tickets) is NOT in `web/supabase/migrations/`** — only 3 incremental migrations are (roles_admin_tooling_a/b, event_status_swap, all 20260611). The foundational tables were applied directly via MCP/dashboard and never synced to the repo, so the schema can't be rebuilt from `web/supabase/migrations/` alone. If the DB is ever truly reset, recovery needs the original DDL (dump it now as an `00000000_init.sql` to be safe).
- **Homepage hardened against a slow/paused DB:** `web/app/page.tsx` wraps `getActiveEvent()` in a 2s `Promise.race` timeout (`activeSlug()`) so the static landing always renders fast even if Supabase hangs; only the CTA slug depends on the DB, falling back to `#event`.
- **sava-pass is now a git repo, pushed to GitHub (2026-06-23): public repo `balautaalex17-sudo/sava-pass`, default branch `main`.** One repo for the whole project (root prototype/docs + `web/` production app). `web/` used to be its OWN nested git repo (5 thin commits + ~52 uncommitted changes) — it was un-nested into the single repo, so `web/`'s prior history is gone (collapsed into the initial commit; current code fully preserved). Local git identity is `Alex <Balautaalex09@gmail.com>` but the GitHub account is `balautaalex17-sudo` — commit author != repo owner.
- **Un-nesting a nested git repo into the parent:** removing `web/.git` is NOT enough — git's index still holds the path as a `160000` gitlink, so `git add -A` refuses to recurse ("Pathspec is in submodule"). Fix: `git rm --cached -f web` (needs `-f` because there's no HEAD yet on a fresh `git init`), THEN `git add web`. A plain `git rm --cached web` errors with "staged content different from both file and HEAD."
- **Creating a GitHub repo with no `gh` CLI:** `gh` isn't installed on this machine but a working GitHub token IS in Windows Credential Manager. Retrieve it with `printf "protocol=https\nhost=github.com\n\n" | git credential fill` (token has `gist, repo, workflow` scopes), then `POST /user/repos` via curl. `git push` then authenticates automatically off the same stored credential — no token in the remote URL needed. For PRs: `jq` is also missing — build the JSON payload with `python -c`/`json.dumps` + `urllib`, not `jq`. Opened PR #1 this way.
- **Immersive v3 rebrand shipped (2026-06-23, branch `feat/immersive-rebrand-membership`, PR #1).** The whole app now wears the dark v3 language EXCEPT the homepage. Two distinct mechanisms by design: homepage `/` = verbatim HTML port (`_immersive/content.ts` + engine); every other route = the v3 look **re-themed in place** onto the existing semantic tokens (NOT a port — they're real data-driven React). The lever: an additive `.theme-immersive` scope in `globals.css` that remaps the semantic aliases (`--bg-app`, `--fg-primary`, `--bg-card`, `--border-*`, shadows) to a new `--im-*` dark palette (ink `#070A12`, cyan `#00A7E8`). Applied on `<body>` in `app/layout.tsx`. Nothing in the frozen `@theme`/`:root` was edited — the rebrand is purely additive. Pages that hardcode slate/white/navy still needed a per-page pass.
- **Re-theming dense inline-style files: `replace_all` per file on whole value strings is the fast path** (`color: "var(--brand-navy)"`→`var(--im-fg)`, `1px solid var(--slate-200)`→`var(--im-line)`, `boxShadow: "var(--shadow-xs)"`→`var(--im-shadow)`). Watch **mixed-target** values: `background: "white"` means card→`--im-ink-2` but button→`--im-ink-3`; `var(--slate-50)` means page-bg→`--im-ink` but thead/track→`--im-ink-3`; `var(--brand-navy)` is text→`--im-fg` but button-bg→`--im-grad`. Do the safe uniform replace_all first, then targeted fixes for the exceptions. On dark, `.btn--navy` was remapped to the gradient centrally (navy is invisible on ink).
- **The buy/scan critical bits on dark:** the ticket QR must sit on its own white plate (`background:#fff;padding:14px`) or it won't scan; the scanner was already dark so it only needed ink/cyan token alignment, not a rebuild (kept it a calm instrument, no added glow).
- **Membership ("Devino membru") flow is LIVE.** New table `membership_applications` (anon INSERT / staff SELECT / admin UPDATE RLS, reuses `is_staff()`/`is_admin()`), `/devino-membru` page (offer + "Cum decurge" timeline + form), `submitApplication` server action (zod, service-role insert, **best-effort Resend email — a failed send never loses an application**, mirrors the ticket-email posture), `/admin/aplicatii` staff review (linked from the admin header), and the landing "Aplică acum" button wired to `/devino-membru` by extending `scripts/extract-immersive.mjs` (re-run it to re-sync; it rewires the button to a static `/devino-membru` `<a>`, no placeholder needed since the route isn't dynamic).
- **Two gotchas during the membership build:** (1) a pre-existing **`applicants` table** (empty, unreferenced orphan from the v2/v3 design exploration, with an `applicant_status` enum) already existed — I created `membership_applications` anyway (purpose-built for the form) and left the orphan untouched; **check `list_tables` before creating**. (2) The v3 "Devino membru" copy says applications close **30 noiembrie / Toamna 2025** — that date is in the PAST relative to 2026, so do NOT server-enforce it or every submission is rejected; it's shown as display copy only and the form stays open.

- **"Whole app looks half-themed" (dark nav, LIGHT body, invisible inputs) in dev = a STALE Turbopack `.next` cache serving a `globals.css` compiled BEFORE the `--im-*` immersive block existed (2026-06-23).** Symptom: every `var(--im-*)` reference falls back to its light value (page bg light, `.input` invisible on light) while `--brand-*` tokens still resolve — because both families are in the same `:root` but the cached chunk was truncated at the immersive boundary. NOT a code bug and NOT a per-page re-theme gap: the source is correct, the cache is stale. **Diagnose by fetching the served CSS chunk, not by reading source:** `curl -s localhost:3000/<route> | grep -oE '/_next/static/[^"]*\.css'` then `curl` that chunk and `grep -c -- '--im-ink:'` — `0` = stale cache (the immersive `:root` block is missing), `1` = fine. **Fix:** force a recompile (any edit to `globals.css`) or, durably, kill dev + `rm -rf .next` + restart. Clean builds and production are unaffected, so this only ever bites the running dev server. Cost a long forensic detour chasing a "light pages" design bug that didn't exist.
- **Headless-Chrome screenshots of a dev build can FALSE-POSITIVE as light/unstyled** even at `--virtual-time-budget=14000` + `--run-all-compositor-stages-before-draw` — but in this case the light render was REAL (stale CSS), not FOUC. Distinguish the two by checking the served CSS chunk (above), not by bumping the budget: if the token is missing from the chunk, no wait time will fix it.
- **"No animations showing" was the visitor's OS reduce-motion setting, not a bug — but diagnose with playwright, never assume.** Every animation path correctly respects `prefers-reduced-motion` (globals.css blanket rule ~L181, homepage `engine-motion.mjs` gate, the `[data-armed]` reveal). On Windows 11 the trigger is Settings → Accessibility → Visual effects → **Animation effects OFF**. Prove it remotely: playwright `newContext({ reducedMotion: 'reduce' })` vs `'no-preference'` and **measure computed opacity over rAF while scrolling an element into view** — normal `.ev-feat` went 0.3→1, reduce stayed min=max=1 (dead). `min==max==1` = reduce-motion suppression, not a broken animation.
- **The live pages only had ONE-SHOT load animations, no scroll-reveal (until 2026-06-23).** `anim-rise/anim-rise-fast/anim-pop/anim-fade` are CSS keyframes that fire once on mount — below-the-fold content finished before you scrolled to it, so you never *saw* it appear. `<Reveal>` (the IntersectionObserver `data-armed` component) existed but was **only used in `app/_archive/landing-v2.tsx` (dead)**; only the homepage `/` had true scroll-reveal (`engine-motion.mjs` `inView`). Grep `<Reveal` (component) separately from `anim-` (classes) — different mechanisms.
- **App-wide inner-page scroll-reveal = `components/ui/ScrollReveal.tsx` (mounted once in `layout.tsx`), variant-based version (2026-06-23).** Reveals `.anim-rise/.anim-rise-fast` as they enter the viewport (`IntersectionObserver` `rootMargin:'0px 0px -10% 0px', threshold:0` — trigger ~90%, a touch before fully in view so it reads as locked to scroll), via `el.animate(...)` WAAPI. **Picks a per-element variant for a layered feel** (`variantFor`): `scale` (img/poster/qr/media → zoom-settle 1.06→1, 820ms), `pop` (SPAN/chip/badge/perk/stat → scale .9→1 with `back.out`, 460ms), `row` (TR/`.row-hover` → small rise 460ms), `rise` (default cards/sections → translateY 26px, 620ms); easing `cubic-bezier(.22,1,.36,1)` except pop. **Stagger by sibling index ×55ms, cap 6.** Earlier version was too slow/laggy (940ms + up to 640ms stagger ≈ 1.6s, trailed the scroll) — short durations + small stagger fixed "laggy / not in sync." On finish: `commitStyles()` + `cancel()` to release held WAAPI animations (keeps long pages smooth). A `setTimeout(900)`+`load` **sweep** reveals any in-viewport target the observer missed (fixes "some elements don't appear"). `MutationObserver` on `document.body` (skips `.sp-immersive-root` nodes) catches client-nav routes + dynamically-added rows. `anim-pop/anim-fade` (transient feedback) left as on-mount CSS. `reduce` is a fallback only — the matchMedia shim makes it false.
- **Make `.sr-shown` alone define the final visible state (opacity:1), no transform in the gate.** The gate is just `html.sr-on .anim-rise{animation:none!important;opacity:0}` — the variant's WAAPI drives the transform (a fixed gate `translateY` would fight the pop/scale variants and, if an animation got interrupted mid-flight, could strand the element). Because `.sr-shown` carries the final state independent of the animation, an interrupted/cancelled WAAPI can never leave an element stuck hidden. Verified across two pages: scrolling through → `stuck: 0`, `sawMidFlight: true`.
- **No-flash reveal gate = pre-paint inline `<script>` + graceful failsafe.** `SCROLL_REVEAL_BOOT` in `layout.tsx` adds `sr-on` to `document.documentElement` before first paint; CSS `html.sr-on .anim-rise{animation:none!important;opacity:0}` hides entrances until JS reveals (`.sr-shown` → opacity 1). A `setTimeout(4000)` strips `sr-on` if `window.__srReady` was never set (component never booted) so content can't get stuck hidden; with JS fully off, `sr-on` is never added and the load animation just plays.
- **A pre-paint script mutating `<html>` className triggers a Next hydration mismatch — fix with `suppressHydrationWarning` on that element.** Adding `sr-on` pre-hydration made server vs client `<html>` className differ ("A tree hydrated but some attributes...didn't match", "won't be patched up"); the first diagnostic showed `errors: []`, so it was newly introduced. `suppressHydrationWarning` on `<html>` is the standard fix (same pattern theme-toggle scripts use) — suppresses only that element's attribute warning, intended client value stays.
- **The homepage has TWO motion layers, BOTH gated on reduce-motion — toggle them together or you get a half-broken hybrid.** `engine.js` (GSAP: hero mask reveal, scrubbed parallax, seam draws, phone, snap-lock, count-up) gates its whole block behind `if(window.gsap && !reduce)` (~L155); `engine-motion.mjs` (Framer Motion `inView`: ev-feat/map/archive/stats/jointeam) gates on its own `prefersReduced`. Forcing ONLY one on (I briefly set engine-motion's gate to `false`) makes the Framer reveals fire while the entire GSAP scene stays dead → "very broken." Both are now forced on together via the matchMedia shim (next bullet).
- **"Animations not in sync with the scroll" on the homepage = Lenis driven by a standalone rAF loop instead of GSAP's ticker.** Fix (standard Lenis+GSAP integration, applied in `engine.js` + patched into the extractor's `engineOut`): `if(window.gsap){gsap.ticker.add(t=>lenis.raf(t*1000));gsap.ticker.lagSmoothing(0);}` so smooth-scroll and `ScrollTrigger.update` run on the same frame, plus `ScrollTrigger.refresh()` immediately, on `load`, and at +600ms so triggers aren't measured against an unsettled layout. **Caveat: `window.scrollTo` doesn't drive Lenis, but `page.mouse.wheel(0,dy)` in a loop with waits DOES** (Lenis listens to wheel events) — so Playwright CAN verify Lenis-driven reveals if you wheel-scroll in increments, not via scrollTo. (The sticky `.intro` "never dismissing" WAS a red herring — it's `position:sticky` by design, you scroll the page over it.)
- **The hero headline being invisible (only "— —" + subcopy showing) was a REAL bug, not a Playwright artifact (user screenshot confirmed).** Root cause: `.hline>span` is masked by CSS `transform:translateY(112%)` inside `overflow:hidden`, and ONLY a GSAP tween un-masks it — but it was a **ScrollTrigger PLAY-ON-ENTER tween, and those don't reliably fire their enter toggle under Lenis** (scrub tweens update continuously via `ScrollTrigger.update` and work; count-up and `.rv` use their own IntersectionObserver and work; only the play-on-enter hline tween silently never played). Worse, even a manual `gsap.to('.hline>span',{yPercent:0})` left it at the masked start (GSAP's own transform handling on that element), while a raw `el.style.setProperty('transform','translateY(0)','important')` DID move it. **Fix (engine.js + extractor): drop GSAP for this reveal — a plain `IntersectionObserver` on `#hero` that, on enter, sets a CSS `transition` on each `.hline>span` and flips the inline `transform` to `translateY(0)!important`, staggered per line.** Diagnose "element won't move" by testing a raw inline `!important` set vs the animation lib: if raw works and the lib doesn't, it's the lib's state handling, not a CSS override. Verified revealed `[0,0,0]` after wheel-scrolling the hero into view.
- **When patching the extractor (`scripts/extract-immersive.mjs`), update the `writeFileSync` to use the transformed variable.** The script slices `engine`/`motion`/`css`/`markup`; the writes for `engine.js`/`engine-motion.mjs` used the RAW vars. A transform computed into a new `const motionOut`/`engineOut` is dead until the corresponding `writeFileSync(... , engineOut, ...)` is also changed. (Caught a no-op patch this way.)
- **"Reveals feel slow/laggy as elements appear" on the homepage = animating `filter:blur` (2026-06-23).** The v3 `.rv` scroll-reveal started `opacity:0;transform:translateY(48px) scale(.93);filter:blur(10px)` and transitioned all three over .8s on ~38 elements. **Animating `filter:blur` is GPU-heavy** and is the usual cause of "slow appearing" reveals. Fix (extractor cssOut `.replace`s, then re-run): drop the `blur` + `scale`, shrink travel to `translateY(22px)`, shorten to .6s, add `will-change:opacity,transform` → compositor-only, glides in. (Smoothness reference was the user's local `active/design-fetch/savapassv2/project/SavaPass Immersive v2.html`; v2 also uses a snappier `lerp:0.085` — lowered v3's Lenis `lerp` 0.1→0.085 to match the less-floaty feel.) Default to opacity+transform reveals; never transition `filter`/`box-shadow` on many elements.
- **Phone (and any hero entrance) must trigger on hero-IN-VIEW, not on load.** The source played the phone entrance with `gsap.from(...,{delay:.15})` on load — but the hero sits below the 100vh sticky intro, so it animated off-screen and was static by the time you scrolled to it ("phone doesn't appear"). Fix: `gsap.set(phone,{autoAlpha:0,scale:.9})` then run `gsap.to(...,{autoAlpha:1,scale:1})` from an `IntersectionObserver` on `#hero` (same pattern as the hline fix). gsap DOES reliably animate the phone (unlike `.hline>span`) because the phone's transform is fully gsap-managed with no conflicting CSS base. Verified phone opacity 0 at load → 1 when the hero enters view.
- **Two more homepage lag sources fixed (2026-06-23):** (1) the `.rv` IntersectionObserver was `else e.target.classList.remove('in')` — **re-hiding on exit and replaying every scroll-by** = constant transition churn/jank. Changed to reveal-ONCE (`if(isIntersecting){fire();io.unobserve()}`). (2) The looping hero/footer videos used a **per-frame reverse-scrub** (`requestAnimationFrame` decrementing `video.currentTime` to avoid a loop seam) — seeking video every frame is expensive/janky. Switched to native `loop` (early-`return` before `v.removeAttribute('loop')` so the markup `loop` attr stays), trading a tiny loop seam for smooth scroll. Both patched in `engine.js` + extractor.
- **"Doesn't start at top on refresh" = browser scroll restoration; fix with `history.scrollRestoration='manual'` in the pre-paint boot script.** Browsers default to `'auto'` and restore the prior scroll position on reload, which with Lenis lands you mid-page. Set `history.scrollRestoration='manual'` + `window.scrollTo(0,0)` in `SCROLL_REVEAL_BOOT` (`layout.tsx`, runs at parse before restoration) so a refresh always starts at the top and normal scrolling resumes. Verified: scrolled to 2051px → `reload()` → `scrollY 0`.
- **The homepage card "blink" was NOT inView re-firing — it was the cards having no `opacity:0` base, so Framer Motion's WAAPI reveal painted them at full opacity then snapped to 0 and faded back up (a flash on entry).** Diagnosis that finally nailed it: a per-element opacity TIMELINE during real `mouse.wheel` (Lenis) scrolling showed `1@scroll0 → 0.31 → … → 1` — the drop *from 1* is the blink. Dead ends ruled out along the way: making `inView` fire-once (Motion already unobserves on undefined return; not the cause), and pre-setting inline `opacity:0` (FOUGHT Motion's WAAPI — it reverted the card to 0 on finish → disappear). **Fix that works: stop using Motion `animate` for these reveals entirely and use a CSS-class reveal** (`.im-rv{opacity:0;transform:translateY(26px);transition:…}` + `.im-rv.im-in{opacity:1;transform:none}` in globals.css). `opacity:0` lives in CSS so the card is never visible before reveal, and a CSS transition persists the end state (no WAAPI revert). `engine-motion.mjs` is now a hand-written module (the extractor sets `motionOut` to it, decoupled from the v3 source) that adds `im-rv` at load + a fire-once IntersectionObserver adds `im-in` on enter; growth bars/threads use `.im-bar`/`.im-thread` (scaleX/scaleY) the same way. Verified: blink-catcher count **0**, timeline `0→1` clean. **Lesson: for scroll reveals on elements with no hidden base, a CSS `opacity:0`+transition class beats a JS animation lib — no flash, no revert, and it can't re-fire.**
- **The stale-Turbopack-`.next` cache bit AGAIN: after adding `.im-rv` to globals.css, the class was applied to elements but computed opacity stayed 1 because the served CSS chunk had 0 occurrences of `.im-rv`.** Same signature as the earlier "half-themed" bug: source correct, served chunk stale. Confirm with `curl $(curl -s localhost:3000/ | grep -oE '/_next/static/[^"]*\.css' | head -1)` piped to `grep -c im-rv`. Fix: kill dev + `rm -rf web/.next` + rebuild + restart (a hot-reload alone didn't pick up the new CSS rule). Note the served chunk URL can keep the SAME hash yet have new content after warmup — re-curl after a 2s warmup before concluding it's still stale.
- **FINAL state = motion FORCED ON regardless of OS reduce-motion (user insisted "force animations as they don't show up", 2026-06-23).** This overrode the spec's "respect prefers-reduced-motion" rule on the user's explicit instruction. Forcing-on takes BOTH a JS lever and a CSS lever because the two are independent:
  - **JS lever — shim `matchMedia` before first paint** (`SCROLL_REVEAL_BOOT` in `layout.tsx`): wrap `window.matchMedia` so any `prefers-reduced-motion` query returns `{matches:false,...}` (pass every other query through). One lever forces all JS layers on at once — homepage `engine.js` `reduce`, `engine-motion.mjs` `prefersReduced`, and `ScrollReveal`'s `reduce` all read it. Must run before the engine scripts load (it's an inline `<script>` in `<body>`, runs at parse, before hydration → before ImmersiveEngine injects the CDN/engine scripts). **The shim does NOT affect CSS `@media (prefers-reduced-motion)` — those are evaluated by the rendering engine, not via matchMedia.**
  - **CSS lever — delete the `@media (prefers-reduced-motion: reduce)` suppressors at their source.** globals.css: removed the blanket `*{animation/transition-duration:.01ms!important}`, the 4.4 infinite-anim kill, and the gate's reduce transform-reset. Homepage immersive CSS: the v3 stylesheet has its OWN aggressive reduce block (`*{animation:none!important}`, `.rv{opacity:1!important;transform:none!important}`, `.hline>span{transform:none!important}`, `.gen-bar i{transform:none!important}`) — and crucially those `!important` rules **override GSAP's inline transforms**, so the JS shim alone leaves the hero mask / `.rv` reveals visually dead. Stripped those killer rules via targeted `.replace()`s in `extract-immersive.mjs` (then re-ran it). Verified force-on under a `reducedMotion:'reduce'` Playwright context: inner reveals animate (0→0.84 mid-flight), `.ev-feat` 0.19→1, `matchMedia(...).matches===false`, zero console errors. To restore accessibility: remove the matchMedia shim + re-add the CSS blocks (globals comment documents the exact rule to re-add) + drop the extractor `.replace()`s.

- **Mobile was already ~80% there — "create mobile version" was an audit-polish-tune pass, not a rebuild (plan: `web/docs/plans/2026-06-23-002-feat-mobile-experience-pass-plan.md`).** The v3 immersive homepage shipped responsive breakpoints (520–920px), `.intro{min-height:100svh}`, and `matchMedia('(hover:none)'/'(hover:hover) and (pointer:fine)')` gates for tilt/magnetic/snap — all ported verbatim. Buyer pages are mobile-first. So at 360/390/414 + landscape there was **no overflow and no errors** before touching anything. Measure the real mobile state with a Playwright mobile context (`isMobile:true,hasTouch:true`) BEFORE planning a mobile build — you may find the layout already works and the real gaps are touch-feel, iOS viewport correctness, and staff data-tables.
- **Mobile foundations that paid off (U1):** Next 16 viewport via `export const viewport: Viewport = { width:'device-width', initialScale:1, viewportFit:'cover', themeColor:'#070A12' }` in `app/layout.tsx` (separate from the `metadata` export; `Viewport` type confirmed in `node_modules/next/dist/lib/metadata/types`). globals.css: `.safe-pb/.safe-pt/.safe-px` helpers = `calc(var(--pb,0px) + env(safe-area-inset-*))`; `@media (pointer: coarse){ input,textarea,select{ font-size:16px } }` to kill iOS focus-zoom; `.min-h-screen-dyn{ min-height:100vh; min-height:100dvh }`. Centered full-screen pages (ticket/success/login) → `minHeight:'100dvh'` so the card centers in the visible area, not behind the iOS address bar (`dvh` is universal in 2026).
- **Lenis on touch = `syncTouch:true` + `touchMultiplier` in the constructor (via `extract-immersive.mjs`).** Lenis 1.x does NOT smooth touch by default (`syncTouch:false` → near-native). The user wanted Lenis kept on touch, so the constructor became `new Lenis({lerp:0.085,smoothWheel:true,wheelMultiplier:1.08,syncTouch:true,touchMultiplier:1.6})`. Verified on a touch Playwright context: Lenis boots, the stats reveal fires on `lenis.scrollTo`-driven scroll, both bg videos autoplay (`muted playsinline autoplay` already in the markup), 9 ScrollTriggers, zero errors. Drive Lenis in Playwright via `window.__lenis.scrollTo(y,{immediate:true})` (programmatic `window.scrollTo` doesn't move Lenis).
- **Staff data-tables on mobile = `overflow:auto` + a `minWidth` on the `<table>` (preserve columns, scroll horizontally), NOT card reflow.** AdminClient's second table already used the pattern (`overflow:'auto', minWidth:560`); applied the same to statistici (560), AdminClient first table (520), team (480) by flipping the wrapper `overflow:'hidden'→'auto'` and adding `minWidth`. The EventEditor 5-col program rows (`gridTemplateColumns:'96px 1fr auto auto auto'`) can't be media-queried inline, so add a stable class (`ee-prog-row`) + `@media(max-width:640px){ .ee-prog-row{ grid-template-columns:1fr!important } }` in globals. Scanner was already mobile-aware (`100dvh` container + a `≤860px` stack-to-1-col media query + camera `62dvh`); only needed `padding-bottom:max(14px,env(safe-area-inset-bottom))`. **Staff routes are auth-gated — Playwright hits the login, so authenticated table/scanner mobile rendering still needs a logged-in pass (staff test-account credential needed).**
- **A user screenshot of "broken mobile" (desktop layout crammed onto a phone: left-aligned giant wordmark, body copy cut off mid-word, marquee + side rail clipped both edges) is the signature of `width=device-width` NOT being applied — almost always a STALE CACHED HTML document on the device, not a CSS bug (2026-06-23).** The homepage already renders correctly at 320/360/390/414/430 + landscape (verified: `scrollWidth==viewport` everywhere, hero subcopy wraps, zero text overflow). Diagnose in this order before touching CSS: (1) `curl -s localhost:3000/ | grep -oiE '<meta name="viewport"[^>]*>'` — if the device-width meta is present, the served doc is fine and the user is on a cached pre-fix page (hard-refresh / Empty Cache and Hard Reload fixes it); (2) Playwright at several widths measuring per-element `getBoundingClientRect().right > innerWidth` for **text-bearing** elements only (`p/h1/h2/a/button`) — the decorative `.lane`/`.run`/`.item` marquee + `.phone-orbit`/`.glow`/`.road`/`engine-*` ambient layers + full-bleed `.intro-video`/`.hero-video` ALWAYS report as overflow offenders by design (clipped by `overflow:hidden`), so filter them out or you'll chase non-bugs. The N badge bottom-left in a screenshot = Next dev indicator (not production), so it's a local-dev cached page. Playwright import on Windows: `import pkg from "file:///C:/.../playwright/index.js"; const {chromium,devices}=pkg;` (CJS, needs file:// + default import).
- **sava-pass IS now deployed to Vercel — live at https://sava-pass.vercel.app (2026-06-23).** Project `sava-pass` (`prj_hmPJHCEbYF4vKmq0ymQjRUo8ISFB`) under team `alex-2027's projects` (slug `alex-2027s-projects`, id `team_0qoJuwzkg8jKIm17o3pRuwB0`), **Root Directory = `web`** (no root `package.json`; app is in `web/`). Deployed via Vercel CLI + a user-supplied token (no `.vercel` link or CLI pre-existed; the Vercel MCP can read/deploy but CANNOT create a project, set root dir, or set env vars — so the CLI-with-token path is the only headless option). Sequence that worked, all run from `web/` so cwd = deploy root: (1) `npx -y vercel link --yes --project sava-pass --scope alex-2027s-projects --token "$T"` (creates the project + `web/.vercel/project.json`, auto-detects Next.js, CLI auto-gitignores `.vercel`); (2) push the 9 env vars from `web/.env.local` for BOTH `preview` and `production` via a bash loop `printf '%s' "$val" | npx -y vercel env add "$key" "$envt" --token "$T" --scope ...` (strip `\r` and surrounding quotes; `IFS= read` + `${line#*=}` so base64 `=` padding survives; never echo values); (3) `npx -y vercel deploy --yes --token "$T" --scope alex-2027s-projects`. **Gotcha: the FIRST CLI deploy of a brand-new project targets `production` (not preview) and grabs the `<project>.vercel.app` alias — even without `--prod`.** Build needs the env vars present or it fails at module load (Stripe/Supabase clients instantiate at import). Stored the token in gitignored `active/.vercel-token` to keep it out of command logs, deleted it after. Stripe webhook won't work on this URL (secret points elsewhere) but homepage + all mobile/visual behavior is testable.

- **App-wide perf pass (2026-06-23, plan `web/docs/plans/2026-06-23-003-perf-app-latency-and-jank-plan.md`, commits 421a6d1..ccaff72): homepage 29.0 MB -> 2.6 MB full transfer (-91%), 13.5 -> 1.85 MB initial, LCP 1016 -> 460 ms.** Measure first with `web/scripts/perf-measure.mjs` (Playwright, sums `request.sizes().responseBodySize`, before/after scroll, LCP). Findings + fixes: (1) **The "~50 MB media" was ~29 MB actually loaded** — `step-*.png` (12.6 MB) + `savapass-disintegrate.mp4` (4.8 MB) were ORPHANS not referenced in the v3 source (grep the source before assuming a file loads); deleted them. (2) **Biggest lever = re-encode, not next/image** — the homepage is a verbatim HTML port (`dangerouslySetInnerHTML`), so `next/image` can't wrap its `<img>`. `web/scripts/encode-media.mjs` (sharp, reads canonical `Sava Pass #2/assets/*.png`, writes sized WebP to `public/imersiv/`) cut 10 PNGs 20.7 MB -> 0.99 MB (-95%); ffmpeg (installed via `winget install Gyan.FFmpeg`, binary at `%LOCALAPPDATA%\Microsoft\WinGet\Packages\Gyan.FFmpeg_*\ffmpeg-*\bin\ffmpeg.exe`) recompressed the 2 bg loops 7.4 -> 0.66 MB (`-an -c:v libx264 -crf 31 -movflags +faststart`). The extractor rewrites `.png`->`.webp` + lazy-loads below-fold posters. (3) Self-hosted `lenis`/`gsap`/`ScrollTrigger` under `public/imersiv/vendor/` + `<link rel=preload>` + parallel load (was a serial unpkg.com chain). (4) `app/api/keep-warm/route.ts` + `vercel.json` daily cron (Hobby = once/day max) prevents the Supabase free-tier pause. (5) Homepage -> ISR (`export const revalidate=300`), buyer/checkout -> `force-dynamic` (live stats); event reads cached via `unstable_cache` (admin client — it CANNOT wrap the cookie client). (6) Gated the perpetual QR box-shadow loop off touch.
- **"It lags around the stats section" = the equalizer animated `height` (a LAYOUT property) on 56 bars on an infinite loop (2026-06-23).** `@keyframes eqbar{from{height:7px}to{height:var(--h)}}` on `.eq span` (56 spans built by engine.js) = 56 reflows/frame. Fix: fix each bar's box `height:var(--h)` and animate `transform:scaleY(.16→1)` with `transform-origin:bottom` (already set) + `will-change:transform` — compositor-only, visually identical. Patched in `extract-immersive.mjs` cssOut. **Generalize: animating `height`/`width`/`top` on many looping elements is as bad as `filter`/`box-shadow`; always prefer `transform`/`opacity`.** When the user points at a specific janky section, grep the v3 source for that section's `@keyframes` and check which PROPERTY animates before assuming it's scroll/JS.
- **Next 16 caching API drift (16.2.9): `revalidateTag` now requires a 2nd `profile` arg (`revalidateTag(tag, string|CacheLifeConfig)`) and fails the build with one arg.** From a server action, use `updateTag(tag)` instead (single-arg, read-your-own-writes, the documented server-action invalidation). `unstable_cache` still works with `{ tags, revalidate }` but `{...} as const` breaks its types (`tags` becomes `readonly`, wants mutable `string[]`) — drop `as const`. Read `node_modules/next/dist/docs/` per AGENTS.md before touching caching.
- **The mobile pass hid `.lrail` but MISSED `.dots` — the right-side `01–05` section-nav (2026-06-24, from user device screenshots of `sava-pass.vercel.app`).** `.dots` is `position:fixed;top:50%;right:clamp(14px,2.4vw,30px);mix-blend-mode:difference` with NO mobile-hide, so on phones it floated OVER the event poster / seats bar / "Cumpără bilet" button (the `mix-blend-mode:difference` inverts the numerals against content behind = why "03" read orange/blue over the poster). Fix mirrors `.lrail`: extractor `cssOut.replace` appending `.dots{display:none;}` to the existing `@media(max-width:760px){.lrail{display:none;}}`, then re-run `extract-immersive.mjs`. The dots are `aria-hidden` decorative desktop nav, so hiding loses no function. **Generalize: the immersive layout has FOUR `position:fixed` overlays — `.dots`, `.lrail`, `.rail` (top 2px bar, full-width, fine), `.tele` corners — audit them TOGETHER for a mobile-hide; a fixed side-rail with no margin to live in WILL overlap content on phones.** The hero `.engine-ticket` chips bleeding off-edge are clipped by `.intro{overflow:hidden}` = intentional ambient decoration, NOT a bug (matches the prior "decorative engine-* layers always report as overflow by design" lesson).
- **Verify a homepage immersive CSS fix via the served `/` HTML, NOT a `_next` CSS chunk** — the immersive CSS is injected as an inline route-lifetime `<style>` (dangerouslySetInnerHTML), so `curl -s localhost:3000/ | grep '<rule>'` is the ground truth for what the browser gets (the inlined rule appears ~twice: SSR HTML + hydration payload, both fine). This differs from the app-shell CSS (globals.css → a real `_next/static/*.css` chunk you'd curl separately). A `display:none` media query needs no render to verify: if the query matches the width, the element is gone — served-HTML grep + build pass is definitive, no Playwright needed (and Playwright is NOT installed in `web/node_modules`).
- **Redeploy to `sava-pass.vercel.app` needs the user's Vercel token (deleted after each use, stored in gitignored `active/.vercel-token`).** The Vercel MCP `deploy_to_vercel` runs server-side and deploys the CONNECTED GIT repo, NOT local uncommitted files — so it ships stale code for a local-only fix. The documented working path uploads local files: `cd web && npx -y vercel deploy --prod --yes --token "$T" --scope alex-2027s-projects` (link already at `web/.vercel/project.json`, projectId `prj_hmPJHCEbYF4vKmq0ymQjRUo8ISFB`). No global Vercel CLI auth on this machine, so the token is required each time. **The project has NO git integration — every deploy was CLI-made (`actor: claude-code…`, `gitDirty:1`), so `git push` does NOT deploy; a CLI deploy with `--prod` is the only way to update the production alias. The first CLI deploy auto-targets production; later ones need `--prod` explicitly. Verified live via `mcp Vercel get_project` (latestDeployment.target=production, aliased to sava-pass.vercel.app).**
- **WHY the immersive homepage doesn't "feel mobile native": its phone-range CSS was barely tuned (2026-06-24).** The `@media(max-width:520px)` block overrides almost NOTHING (just `.ev-past` grid + `.ev-cta` full-width); all real responsive work lives at 760–920px. So phones inherit desktop positioning/spacing/type — which is why desktop decoratives overlap content and spacing reads desktop-y. When asked to make mobile native, the gap is the PHONE breakpoint (≤520/≤760), not the tablet one; extend that nearly-empty `≤520px` block. The marquee `.strip` (position:absolute;top:23px) overlapping `.eyebrow` is the SAME pattern as the dots — add it to the `@media(max-width:760px){.lrail{…}.dots{…}.strip{display:none}}` hide. The fixed/absolute viewport-level overlaps to audit together are `.strip` (hero), `.dots`/`.lrail`/`.rail` (fixed), `.tele` corners; everything else (`.engine-*`, phone, `.bokeh/.eq`, `.route/.pin`, `.gen-*`) is section-scoped + contained. Comprehensive whole-app mobile-native polish plan: `web/docs/plans/2026-06-24-002-feat-mobile-native-whole-app-plan.md` (conservative = subtract clutter + tighten + ≥44px/≥16px + full-width CTAs, desktop byte-for-byte intact via mobile-only media queries).

## Token-saving rules

- Grep for screen name before loading any large JSX file.
- **Diagnose "missing styles / wrong theme" in Next dev by curling the served CSS chunk and grepping for the token definition**, before reading source or re-screenshotting. The chunk is ground truth for what the browser actually got; source can be right while the cache serves stale CSS.
- Always run `generate_typescript_types` MCP tool before touching any Supabase `from()` call — stale stubs cost multiple build-fix cycles.
- Build errors in Next.js Turbopack are sequential: fix one, rebuild, fix next. Don't try to fix multiple TS errors at once without seeing the next error first.
- Start the dev server with the Bash tool (`npm run dev` + run_in_background), not PowerShell — PS 5.1 rejects the trailing `&` (ampersand reserved) and wastes a round-trip.
- In PowerShell, `npm.ps1` may be blocked by execution policy; use `npm.cmd run dev` instead to start the app.

## Never do

- Don't fetch the entire 56KB `screens-mobile.jsx` into context when only fixing one screen — `grep` for the screen name first.
- Don't render the prototype to take screenshots unless asked — per HANDOFF.md, source is authoritative.
- Don't try to open `SavaPass Prototype.html` via `file://` — CORS blocks the CDN scripts. Always serve over HTTP.
